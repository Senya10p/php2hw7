PHP-2. Урок 7.
Домашняя работа.

1. Применили генератор в классе Db.  Сделали метод queryEach(), который генерирует запись за записью из ответа сервера БД,
не делая fetchAll(), а построчно исполняя fetch(). Проверили работу метода и использовали в программе.

2. Создали класс AdminDataTable (В App файл AdminDataTable.php).
1) Его конструктор принимает на вход массив моделей (это будут строки таблицы) и массив функций (это будут столбцы).
В App находится файл с функциями (файл functions.php).
2) Метод render() отображает таблицу следующим образом:
  - Для каждой записи (это строка таблицы) последовательно вызываются функции, в них передается запись (модель).
  - То, что вернула функция - становится значением ячейки таблицы.

3. Применили этот класс в своей админ-панели (В App/Controllers файл Admin.php.
Шаблон находится в templates, файл admindatatable.php).
